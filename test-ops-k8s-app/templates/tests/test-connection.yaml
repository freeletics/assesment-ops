apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-ops-k8s-app.fullname" . }}-test-connection"
  labels:
{{ include "test-ops-k8s-app.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "test-ops-k8s-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
