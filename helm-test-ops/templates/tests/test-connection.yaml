apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-test-ops.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "helm-test-ops.name" . }}
    helm.sh/chart: {{ include "helm-test-ops.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-test-ops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
