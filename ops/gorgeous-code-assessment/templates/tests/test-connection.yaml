apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gorgeous-code-assessment.fullname" . }}-test-connection"
  labels:
{{ include "gorgeous-code-assessment.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gorgeous-code-assessment.fullname" . }}:{{ .Values.service.externalPort }}']
  restartPolicy: Never
